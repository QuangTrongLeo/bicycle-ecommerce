import classNames from 'classnames/bind';
import styles from './style.module.scss';
const st = classNames.bind(styles);

function MainFooter() {
  return <h1>MainFooter</h1>;
}

export default MainFooter;
